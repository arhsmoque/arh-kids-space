name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: arhsmoque/arh-kids-space/.github/workflows/reusable-cf-pages-deploy.yml@main
    with:
      project-name: arh-kids-space
      directory: .
    secrets: inherit
