# Cloudflare credentials — copy to ~/.cf-credentials and fill in values
# This file is a template only. ~/.cf-credentials is never committed.
#
# Usage:
#   cp scripts/cf-credentials.example ~/.cf-credentials
#   chmod 600 ~/.cf-credentials
#   nano ~/.cf-credentials   # fill in values
#
# Then run:
#   ./scripts/new-cf-project.sh <repo-name>

export CLOUDFLARE_API_TOKEN="cfat_..."
export CLOUDFLARE_ACCOUNT_ID="dc3bfa957bdf216b7cc45214455aaa72"
